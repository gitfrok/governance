# CLAUDE.md — governance (control surface)

**Read `AGENTS.md` in this repo first — it is canonical.** This repo IS the Source of Truth.

- `docs/adr/` is SoT (ADR-0001) — supersede, never edit Accepted ADRs.
- Changes to ADRs, specs, invariants, `contracts/`, `policies/` happen ONLY here, reviewed via PR.
- Contracts/policies are additive-only within v1. New decision → Proposed ADR and stop.
- Follow AGDD (`docs/process/agdd.md`); every PR meets `.github/pull_request_template.md` + `docs/process/definition-of-done.md`.

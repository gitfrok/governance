# Architecture Decision Records (ADRs) — Source of Truth

Every architecturally-significant decision lives here as an ADR. **If any diagram, wiki, slide, design
doc, or comment disagrees with an Accepted ADR, the ADR wins.** Narrative context is in
`../architecture/`, the shared surface in `contracts/`; the *decisions* are here.

## Process (ADR-0001, ADR-0002)

Nygard/MADR style — *Status, Context, Decision, Consequences, Alternatives* — one decision per file;
see [`0000-template.md`](0000-template.md). Statuses run
`Proposed` → `Accepted` → (`Superseded by ADR-XXXX` | `Deprecated`).

1. Copy the template to `NNNN-title.md`, taking the next number.
2. Open a PR — **the PR review is the decision-approval gate.**
3. On merge, set the status to `Accepted`. **Accepted ADRs are immutable.**
4. To change a decision, add a **new** ADR that supersedes the old one and mark the old one
   `Superseded by ADR-XXXX`. Never rewrite history.

## Index

| ADR | Decision | Status |
|-----|----------|--------|
| [ADR-0001](0001-adrs-as-source-of-truth.md) | ADRs are the Source of Truth | Accepted |
| [ADR-0002](0002-governance-driven-design.md) | Adopt Governance-Driven design | Accepted |
| [ADR-0003](0003-multitenancy-shared-db-rls-cells.md) | Multi-tenancy: shared DB + RLS + cells | Accepted |
| [ADR-0004](0004-git-storage-tier.md) | Git storage via Git-RPC over sharded nodes | Accepted |
| [ADR-0005](0005-ci-ephemeral-sandbox-isolation.md) | CI: ephemeral per-job sandbox isolation | Accepted |
| [ADR-0006](0006-policy-as-code-opa.md) | Policy-as-code with OPA (PEP/PDP) | Accepted |
| [ADR-0007](0007-append-only-audit-log.md) | Append-only, tamper-evident audit log | Accepted |
| [ADR-0008](0008-flat-rate-fair-use-cost-governance.md) | Flat-rate + fair-use cost governance | Accepted |
| [ADR-0009](0009-byo-infrastructure-cp-dp-split.md) | BYO infra: control/data-plane split (BYOC) | Accepted |
| [ADR-0010](0010-target-gke-eks-aks-portability.md) | Target GKE/EKS/AKS via portability layer | Accepted |
| [ADR-0011](0011-outbound-only-agent.md) | Outbound-only agent for CP↔DP | Accepted |
| [ADR-0012](0012-gvisor-default-ci-isolation-byo.md) | gVisor default CI isolation on BYO | Accepted |
| [ADR-0013](0013-helm-plus-operator-packaging.md) | Package via Helm + Operator | Accepted |
| [ADR-0014](0014-dedicated-code-search-index.md) | Dedicated code-search index (Zoekt-style) | Accepted |
| [ADR-0015](0015-uiux-github-clean-unified-security.md) | UI/UX: GitHub-clean + unified security surface | Accepted |
| [ADR-0016](0016-git-failover-sync-one-replica.md) | Git failover: sync 1 replica + async fan-out | Accepted |
| [ADR-0017](0017-agent-grpc-mtls-protocol.md) | Agent protocol: gRPC streaming over mTLS | Accepted |
| [ADR-0018](0018-dual-loss-failover-policy.md) | Dual-loss failover: fail-safe (halt + audited override) | Accepted |
| [ADR-0019](0019-technology-stack.md) | Technology stack (v1) | Superseded by ADR-0020 |
| [ADR-0020](0020-technology-stack-rev2.md) | Technology stack rev.2 (Astro SSR, Redpanda, SeaweedFS) | Superseded by ADR-0023 |
| [ADR-0021](0021-local-dev-orbstack.md) | Local/dev: OrbStack + *.orb.local | Superseded by ADR-0024 |
| [ADR-0022](0022-high-cohesion-low-coupling.md) | Modular architecture: high cohesion, low coupling | Accepted |
| [ADR-0023](0023-technology-stack-rev3.md) | Technology stack rev.3 (version floors + Valkey) | Superseded by ADR-0089 |
| [ADR-0024](0024-local-dev-minikube.md) | Local/dev: Minikube only + *.gitsaas.test | Accepted |
| [ADR-0025](0025-modular-monolith.md) | Modular monolith (one app binary per plane) | Accepted |
| [ADR-0026](0026-service-based-target.md) | Target architecture: service-based (coarse services) | Accepted |
| [ADR-0027](0027-repo-topology-submodules.md) | Multi-repo source topology via git submodules | Accepted |
| [ADR-0028](0028-agdd-framework.md) | AGDD — AI-Agent Governance-Driven Development framework | Accepted |
| [ADR-0029](0029-imported-history-attested-provenance.md) | Imported history is attested, not audited — two-class provenance | Accepted |
| [ADR-0030](0030-extraction-trigger-budgets.md) | Extraction-trigger budgets for the modular monolith | Accepted |
| [ADR-0031](0031-merge-enforcement-split-rulesets.md) | Split merge enforcement — bind admins to checks, keep review bypassable | Superseded by ADR-0053 |
| [ADR-0032](0032-contract-schema-gates.md) | Gate the contract schema — lint + breaking checks on `contracts/` | Accepted |
| [ADR-0033](0033-git-storage-backing-block-volumes.md) | Live bare repos stay on block volumes — SeaweedFS-FUSE fails git's rename contract | Accepted |
| [ADR-0034](0034-image-pins-are-resolvable-patch-tags.md) | Image pins are fully-qualified, resolvable, patch-level tags | Accepted |
| [ADR-0035](0035-first-party-images-scratch-digest-signed.md) | First-party images: `scratch` base, digest-referenced, cosign-signed | Accepted |
| [ADR-0036](0036-resolvability-over-vendor-registry.md) | When a vendor registry cannot be verified, prefer the one that can | Accepted |
| [ADR-0037](0037-rdf-agent-surface-generation.md) | Agent surfaces are generated from governance (RDF canonical-first), not hand-maintained | Accepted |
| [ADR-0038](0038-governance-gplv2-vendor-rdf.md) | `governance` is GPL v2 and vendors RDF; the code repos' licence stays open | Superseded by ADR-0039 |
| [ADR-0039](0039-no-vendored-third-party-code.md) | Vendor no third-party code; the surface pipeline is ours and `governance` keeps GPL v2 by choice | Accepted |
| [ADR-0040](0040-apache-2-across-the-tree.md) | Apache-2.0 across the whole tree | Accepted |
| [ADR-0041](0041-git-http-ssh-front-doors.md) | Git HTTP and SSH front doors terminate in the data plane | Accepted |
| [ADR-0042](0042-replica-promotion-fencing.md) | Replica promotion uses monotonic fencing terms | Accepted |
| [ADR-0043](0043-credential-verifier-lookup.md) | Resolve opaque credential verifiers through a narrow RLS gateway | Accepted |
| [ADR-0044](0044-cosign-signing-key-custody.md) | Cosign signing-key custody and rotation for first-party images | Accepted |
| [ADR-0045](0045-zitadel-tenant-principal-mapping.md) | Zitadel verified claims map to tenant-scoped principals | Accepted |
| [ADR-0046](0046-platform-operator-force-promote-authz.md) | Platform-operator principals authorize replica force-promotion | Accepted |
| [ADR-0047](0047-first-party-image-distribution.md) | First-party release images are publicly pullable; trust is verified offline | Accepted |
| [ADR-0048](0048-git-storaged-image-base.md) | `git-storaged` ships on a minimal base with git, not `scratch` | Accepted |
| [ADR-0049](0049-bff-browser-session.md) | The browser session is an opaque server-side cookie the BFF owns | Accepted |
| [ADR-0050](0050-large-objects-over-seaweedfs-fuse.md) | Large objects (LFS, CI artifacts, container images) are served from a SeaweedFS FUSE mount | Accepted |
| [ADR-0051](0051-seaweedfs-mount-produced-by-node-daemonset.md) | The SeaweedFS FUSE mount is produced by one privileged node DaemonSet, not by a sidecar in each pod | Accepted |
| [ADR-0052](0052-bff-owns-its-session-store.md) | The BFF may open exactly one datastore — its own session store — behind a declared waiver | Accepted |
| [ADR-0053](0053-direct-to-main-ci-is-the-gate.md) | Work lands directly on `main`; CI on push is the only gate | Superseded by ADR-0054 |
| [ADR-0054](0054-main-guard-only.md) | `main` is guarded against rewrite and deletion, and against nothing else | Accepted |
| [ADR-0055](0055-audit-retention-attested-imported-records.md) | Audit retention: the chain never removes; attested history expires; a pack is a snapshot | Accepted |
| [ADR-0056](0056-owasp-aisvs-adoption.md) | OWASP AISVS — bound to the agentic SDLC and to AI-enabled features, at L3 with one named exception | Accepted |
| [ADR-0057](0057-ai-assisted-review-openai-compatible.md) | AI-assisted review via a tenant-configured OpenAI-compatible endpoint; AISVS L3 minus sender-constrained credentials | Accepted |
| [ADR-0058](0058-woodpecker-pipeline-format.md) | Adopt Woodpecker's pipeline format — the syntax, not the engine | Accepted |
| [ADR-0059](0059-ci-scan-results-to-findings.md) | How a CI scan's results reach the findings plane — the runner persists, a subscriber ingests | Accepted |
| [ADR-0060](0060-agent-enrolment-identity.md) | Agent identity: one-time enrolment token, control-plane-issued short-lived certificates | Accepted |
| [ADR-0061](0061-metering-authority.md) | The control plane is the authority for fair-use metering | Accepted |
| [ADR-0062](0062-durable-agent-residency-stores.md) | Durable Postgres stores for agent enrolment, registry and residency behind the existing ports | Accepted |
| [ADR-0063](0063-residency-declare-wire-surface.md) | Residency Declare is a control-plane admin gRPC surface; the agent channel never declares | Accepted |
| [ADR-0064](0064-agent-ca-key-custody.md) | Agent-CA keys in platform-secrets custody; rotation by staged trust bundle, no re-enrolment | Accepted |
| [ADR-0065](0065-multi-cluster-byo-readiness.md) | Multi-cluster BYO: signed operator image, N data planes per tenant, aggregate metering | Accepted |
| [ADR-0066](0066-openbao-control-plane-custody-service.md) | OpenBao as the control-plane custody service (agent-CA first consumer, transit signing, Shamir unseal) | Accepted |
| [ADR-0067](0067-platform-operator-residency-declaration.md) | A tenant-scoped platform operator may set a tenant's residency declaration (extends ADR-0046's role by one action) | Accepted |
| [ADR-0068](0068-redpanda-durable-issuance-retry.md) | Redpanda-backed durable issuance retry — a custody outage never interrupts enrolment (Phase 3.2 candidate; would supersede SPEC-0042 AC6's interim release-the-claim posture) | Proposed |
| [ADR-0069](0069-cvd-first-design-system.md) | The product design system is CVD-first, token-only and light by default — discharges ADR-0015's design-system follow-up; diffs become blue/orange with gutter markers | Superseded by ADR-0091 |
| [ADR-0070](0070-full-product-surface.md) | The prototype's six absent surfaces become product, gated by a route-before-pixel law — supersedes SPEC-0047's scope record; PR-24…PR-32 now in the PRD | Accepted |
| [ADR-0071](0071-repository-registry-durability.md) | The repository registry is durable and is the product's truth for existence — the Postgres adapter owed since T-0004; a repo on disk with no row is absent by consequence, not by defect | Accepted |
| [ADR-0072](0072-ci-job-history-and-logs.md) | CI keeps a durable job history; job logs are a separate decision it does not make — PR-26 is delivered in half, deliberately | Accepted |
| [ADR-0073](0073-tenant-policy-authoring.md) | Policy authoring needs a per-tenant policy source governance does not have; a tenant policy may only ever narrow | Accepted |
| [ADR-0074](0074-issues-context.md) | Issues is a bounded context, and the largest permanent widening proposed — Accepted with the recommended alternative: no tracker is built, a merge request references an external one by identifier and link | Accepted |
| [ADR-0075](0075-releases-context.md) | Releases is a bounded context whose promises are supply-chain promises; tenant signing does not reuse the release trust bundle; Accepted with the tags+notes increment | Accepted |
| [ADR-0076](0076-repository-settings-context.md) | Repository settings is where policy quietly becomes a UI toggle, which PR-10 forbids; visibility and deletion deferred; Accepted with the name/description/archival increment | Accepted |
| [ADR-0077](0077-admin-area-context.md) | The admin area reaches the audit log through a grant, not a role; `admin` is not a new authorization primitive; Accepted with the dated-fleet-report + grant-door increment, members deferred | Accepted |
| [ADR-0078](0078-marketing-surface-separation.md) | The marketing page is served by a surface that never holds a session, on a different origin; Accepted — nothing to move, the root is guarded instead | Accepted |
| [ADR-0079](0079-dimensional-tokens-and-one-page-shell.md) | Spacing and type join the token layer and one shell owns page geometry — extends ADR-0069's colour-only token law; 347 dimensional literals, 10 font sizes, 3 content widths measured; Accepted as written | Accepted |
| [ADR-0080](0080-code-review-durability.md) | Merge requests, reviews and branch protection become durable — the gap ADR-0071 closed for the registry; the adapter scopes from whichever source carries the tenant and refuses a disagreement; Accepted as written | Accepted |
| [ADR-0081](0081-ci-job-logs-deferral.md) | CI job logs remain deferred — the reopen trigger is a user asking, an observed bypass, or an owned redaction capability, not the PRD row; capture and residency are fixed now and PR-26's scoping clause becomes an access floor, so the reopen starts two questions smaller; check 13 keeps holding the absence at the wire | Accepted |
| [ADR-0082](0082-tenant-policy-authoring-deferral.md) | Tenant policy authoring remains deferred — reading A stays the only authoring path; the reopen trigger is a named tenant need, not the PRD row; check 14 keeps holding the absence at the wire | Accepted |
| [ADR-0083](0083-release-artifacts-deferral.md) | Release artifacts stay deferred until a user asks — the reopen trigger is named (a user, a custody signing consumer, or import parity) and the shape is fixed now (tenant-scoped SeaweedFS blob, own retention class, ADR-0075's trust rules survive) so the reopen is one decision smaller | Accepted |
| [ADR-0084](0084-version-guard-and-the-projection-write.md) | Save has two shapes and the version guard can only guard one — the event path's version-preserving projection write conflicts with AC9's guard on every push; the write splits (guarded Save for bumped writers, a version-preserving projection method for the event path), the conflict gets its error back, Merge's save runs before its ref move with a named compensation, and the idempotency insert converges; SPEC-0061 amended and T-0078's RED resumes on acceptance | Accepted |
| [ADR-0085](0085-four-eyes-floor.md) | Every merge lands through review and the floor is four eyes — two non-author approvals on every merge, protected or not; the author's review never counts; the floor lives in the bundle beside required_approvals where no caller reaches and no refactor silently drops it | Accepted |
| [ADR-0086](0086-notifications-context.md) | Notifications are their own context fed by the bus — in-app list + unread count first, recipients derived server-side from the events' own aggregates, idempotent on event ID; email and webhooks are named follow-ups, not silent gaps | Accepted |
| [ADR-0087](0087-draft-merge-requests.md) | A merge request can be a draft — DRAFT joins the state machine additively, MarkMergeRequestReady is its one door out, and while DRAFT the machinery stays quiet: no projections, no announcements, no merges | Accepted |
| [ADR-0088](0088-merge-strategies.md) | Merges are feature-based by default — per-repository merge_commit/squash/rebase chosen server-side; trunk-based landing is a mode that constrains history shape (linear only) and never widens who may land what; commit-producing work lives in git-storaged | Accepted |
| [ADR-0089](0089-technology-stack-rev4.md) | Technology stack rev.4 — the Go floor moves 1.26 → 1.27; one floor changes, every other ADR-0023 floor and stack choice carries forward; the Go builder base moves 3.22 → 3.23 with it because Alpine 3.22 carries no 1.27 image (build stages only; git-storaged's shipped alpine:3.22.2 runtime is untouched); the touchpoints are enumerated per repo (ADR-0027 keeps them three commits) and the 1.27 idiom sweep is SPEC-0036's follow-up, not this ADR | Accepted |
| [ADR-0090](0090-git-storaged-runtime-base-for-replay.md) | git-storaged's runtime base moves to Alpine 3.24.1 so the rebase landing works — the shipped base carries git 2.49, which rejects the --ref/--ref-action pair AC4's replay depends on, so every rebase and trunk-fallback landing refuses on the image we publish; also implements ADR-0048's decision 4 (the base pin has never had a floor or a gate) and adds the capability to what its build proves | Accepted |
| [ADR-0091](0091-ui-kit-as-the-token-source.md) | The ./UI kit becomes the design system's source of truth, reversing ADR-0069 decision 6 — it moves into webfrontend (invariants 21–25), the source is an authored tokens.json rather than the 179 KB comp, and tokens.css becomes generated output behind a freshness gate; records the measured cost: the kit declares 17 custom properties against the governed layer's 87, overlapping in 9 names, so this is an authoring task and a naming reconciliation, not a promotion | Accepted |
| [ADR-0092](0092-gcp-as-the-first-party-cloud-provisioned-by-opentofu.md) | GCP is the first-party cloud, provisioned by OpenTofu + Terragrunt | Accepted |
| [ADR-0093](0093-control-plane-chart.md) | The control plane installs from its own chart, `deploy/helm/gitfrok-controlplane` — not an extension of ADR-0013's data-plane chart, whose no-inbound and no-secret properties are gated and are the control plane's inverse; the chart renders the three first-party workloads and no stateful service, carries no secret, and never promises a running control plane because ADR-0066's quorum unseal keeps a human in the cold-start path; writes down the cp/dp component partition `deploy/dev` has been withholding, and surfaces the undecided cross-plane read path that stops the browser surface working across two clusters | Accepted |
| [ADR-0094](0094-cross-plane-read-path.md) | The repository surface is served data-plane-side, behind a door the customer exposes — the gap underneath ADR-0093's: the BYO chart renders no Service and `git.gitsaas.test` exists only in `deploy/dev`, so `git push` was as unreachable as a file view and the door itself had never been decided. The chart still opens nothing; the customer exposes one door serving Git and the repository surface alike, a `bff`+`webfrontend` pair moves data-plane-side by route partition, and cross-plane navigation is a URL rather than a proxy — so source never enters a vendor process and ADR-0011 stands unamended. Amends ADR-0093's partition | Accepted |
| [ADR-0095](0095-control-plane-ingress-tls-dns.md) | The public control-plane surface gets three flat names in the Cloudflare `7.solutions` zone as directed — `app-gitfrok`, `auth-gitfrok`, `agents-gitfrok` — and the shipped agent decides the rest of the shape: `platform/agentclient` documents `Roots` as the **pinned** pool verifying the gateway's **server** certificate as well as the client ones, and `GITFROK_AGENT_CA_BUNDLE` is `require()`d, so a publicly trusted or Cloudflare-terminated certificate on the agent door is untrusted by every agent, and an identity-forwarding proxy would re-open SPEC-0002 limit (d) that SPEC-0043 AC6 closed. Browser and Zitadel issuer (ADR-0045 binds `ExternalDomain`) share a Gateway API listener on GKE's managed controller with ACME **HTTP-01** certificates — chosen so that **no Cloudflare credential enters the cluster**, since flat sibling names make the company apex the only scope a token can have; `agents-gitfrok` is a separate L4 passthrough address, permanently DNS-only, with a server certificate from the ADR-0060/0066 CA rotating under SPEC-0044. Both addresses are reserved static IPs, because an ephemeral one silently breaks a hand-written record. No `external-dns`; the existing 37-hex Cloudflare **Global API Key** is account-root and is barred from cluster, OpenTofu and Secret alike. Amends ADR-0092 decision 4 (Cloud DNS leaves the four-API set and the `dns-zone` unit retires); discharges its ingress follow-up and closes ADR-0093 decision 6's seam | Accepted |
| [ADR-0096](0096-kustomize-only-installers.md) | Kustomize is the only installer technology and Helm leaves the tree, as directed. The control plane is the cheap half — ADR-0093's chart was never built, no spec or task ever referenced it, and its own decision 7 ('vendor-internal', 'not a distributed artifact') had already removed Helm's packaging advantage — so the engine is chosen rather than changed, at `deploy/k8s/controlplane/`. The data plane is the expensive half and none of that reasoning transfers: that chart is distributed, signed under ADR-0044, gated by eight `check-byo-chart.sh` assertions, and its helm-rendered rows 11 and 14 went green only on 2026-08-21 after being NOT RUN for most of the chart's life — so this ADR **destroys that evidence**, sends those rows back to 'not run' with itself as the cause, and requires SPEC-0039 to be amended. Kustomize has no package, so decision 6 replaces Helm's: the release artifact becomes the **rendered** manifests, signed on ADR-0044's existing terms and applied by ADR-0013's Operator, which survives unchanged — a gain for verify-before-apply, since what is signed is now what is applied. `secretGenerator` is forbidden on both planes with a gate, because it is the idiomatic front door to the one thing ADR-0093 decision 3 forbids. Amends ADR-0093 decisions 1 and 5 and ADR-0013's packaging half; `deploy/dev` is untouched | Accepted |
| [ADR-0097](0097-operator-access-via-cloudflare-zero-trust.md) | Operator access to the **Kubernetes API** goes through Cloudflare Zero Trust and both clusters' endpoints go private — reversing the unrestricted public endpoint accepted hours earlier the same day. The connector cannot live in the cluster, and not by preference: reaching a private API to install the workload that grants access to that API requires the access it has not yet granted. So it is a GCE VM, which ADR-0092 decision 4 permits OpenTofu to create where a Deployment would have been an ownerless workload. Narrows ADR-0095 decision 8 honestly rather than pretending it is untouched: a **tunnel** credential now exists on a VM, while the **DNS** credential that decision was written about — write over the whole `7.solutions` apex — stays barred, and HTTP-01 issuance is unchanged. Cheap because Zero Trust is already in production here (6 tunnels, 17 Access apps, 2 IdPs, verified 2026-09-22); costly because it puts the product's DNS and the operators' ability to respond to an outage in one failure domain, so decision 5 writes down a break-glass. **Does not touch ADR-0095's three public hostnames** — customers are not going behind Access, and the agent door cannot. Amends ADR-0092 decision 2 | Accepted |
| [ADR-0098](0098-artifact-registry-is-the-publish-target.md) | Artifact Registry replaces `ghcr.io/gitfrok` as the publish target for first-party images, and the repository is made **publicly readable on purpose** — because ADR-0047's public pullability is what lets a BYO customer pull without a vendor credential, GHCR packages are public by a flag, and Artifact Registry repositories are private by default. Written because **nothing is published anywhere**: ADR-0047 names ghcr.io, `versions.env` names docker.io (404 on all four images), the Artifact Registry repository is empty, and `check-dev-images.sh` exempted first-party images from registry resolution — right for a laptop, and it hid the gap the moment a real cluster existed. Every pod the installer renders would land in ImagePullBackOff, so this is the FIRST blocker in the chain, not a later one. Signing is untouched: ADR-0044 signs `oci_ref@digest`, so only the ref changes. The cost is portability — ADR-0010 protected a cloud-neutral registry, and a BYO install on EKS or AKS now pulls from Google, with our egress bill. Amends ADR-0047's registry choice only; discharges ADR-0092's Artifact Registry follow-up | Accepted |
| [ADR-0099](0099-third-party-stateful-set.md) | The third-party stateful set — Postgres, Valkey, Redpanda, OpenBao, Zitadel, SeaweedFS — installs from a Kustomize tree of its own at `deploy/k8s/platform/`, kept OUT of the control-plane overlay because ADR-0093 decision 2 made these inputs rather than contents and merging them would make every Postgres bump a control-plane release. `deploy/dev` is not the starting point it looks like: exactly one of the six is production-shaped (OpenBao's 3-node Raft), and the other five are single-replica **Deployments with a ReadWriteOnce PVC** — a shape that cannot roll, because the new pod cannot attach the volume the old one holds. So all of them become StatefulSets, and replica counts are decided per component with the small ones argued rather than defaulted (Valkey stays at 1 on purpose: losing sessions logs everyone out, which is recoverable in a way losing Postgres is not). **CloudNativePG is the one operator adopted**, for the one component whose failure is unrecoverable, and it installs from plain YAML so ADR-0096's no-Helm rule holds. Amends ADR-0092 decision 5 narrowly: a GCS bucket for Postgres BACKUPS — that decision refused a bucket *for blobs*, and its own negative had already accepted the backup obligation. Also makes `deploy/k8s/controlplane/base`'s `https://openbao:8200` true, which was written against a production TLS posture that did not exist. Discharges ADR-0092/0093's stateful-set row | Accepted |
| [ADR-0100](0100-plane-ownership-of-served-contexts.md) | Which plane serves which context. SPEC-0070 stopped on what looked like a migration of nine bounded contexts — `GITFROK_PDP_ADDR` is the data plane's ENTIRE door (ADR-0041) and 14 of the BFF's 15 clients ride it. Measuring the composition roots shows it is not: the module partition already matches ADR-0094 decision 4, `audit`/`identity`/`policy` are wired into BOTH planes, and the control plane already constructs an OPA decision point, a Postgres audit trail and an identity authenticator in-process. What is missing is **door registration**, not contexts — so the fix is four `Register…Server` calls on an existing listener (`PolicyDecisionPoint`, `EvidenceService`, `AuditorGrantService`, `OIDCLogin`), which makes the owner's shape (a) the cheapest of the four rather than a standalone favour. Also settles what ADR-0094 left unassigned — the repository list and settings are data-plane surfaces — and records that bi-planar modules are the design, not drift, so nobody 'fixes' them into a shared service and breaks ADR-0011. Amends ADR-0094 decision 4: notifications stay data-plane-side, because their events originate in code review and CI and the only cross-plane channel carries desired state, not product events. `GITFROK_PDP_ADDR` is renamed, because a name asserting one service while carrying fourteen is how this was mis-sized | Accepted |
| [ADR-0101](0101-schema-ownership-per-plane.md) | A schema belongs to the plane whose modules use it. Written after T-0088 stopped on ADR-0100's "only registration is missing", which the composition root refuted. Measured this time: **there is one database today** — both dev planes carry the same `GITFROK_DATABASE_URL` and `dev-provision.sh` applies twelve migrations to it — so ownership has had no way to surface, exactly as ADR-0093 said the single namespace hid the component partition. Production is already two (ADR-0092/0099). Eleven schemas: eight resolve to one plane by module wiring; **`audit`, `identity` and `policy` exist in BOTH with disjoint rows**, which is ADR-0100 decision 4's bi-planar modules followed through to storage — two instances of one shape holding different facts, never replicated and never joined across the boundary. The payoff: T-0088's three missing stores all live in bi-planar schemas, so **no data migrates** — it needs three constructors and an OIDC config. The cost, stated: grants and decision records join the audit trail in being split with nothing joining them, so an evidence pack assembled on one plane is structurally incomplete about the other | Accepted |
| [ADR-0102](0102-data-plane-session.md) | A data-plane deployment authenticates for itself, against the same issuer. SPEC-0070 routed the planes correctly and left one thing routing cannot fix: a data-plane BFF could serve the repository surface and authenticate nobody, because ADR-0093 put Valkey control-side, ADR-0100 moved `OIDCLogin` control-side, and ADR-0011 forbids reaching across for either. The obvious fix — a control-issued credential verified offline with the shared `gitfrok-pat-verifier` — **fails on measurement**: ADR-0043 resolves credentials through a DATABASE gateway and ADR-0101 decision 2 gives each plane disjoint identity rows, so a control-issued PAT is not present data-side. What works is that the issuer is already public (ADR-0095 gave it its own hostname): the data plane runs its own OIDC flow against `auth-gitfrok`, validates against its JWKS, and holds sessions in its own Valkey — outbound only, the direction ADR-0011 permits. One login, because the ISSUER is shared and not the session. Amends ADR-0093: Valkey joins audit/identity/policy as bi-planar. Cost, stated: a customer's browser surface now depends on the vendor's issuer being reachable, though Git over SSH and PAT-authenticated HTTP are not | Proposed |
| [ADR-0103](0103-byo-installer-crd-lifecycle-and-bundle-payload.md) | The BYO installer's CRD is applied separately, and the signed bundle needs a payload the tree does not have. Written because SPEC-0068 refused to leave Draft with two decisions open, and one of them turned out to be a contradiction. **The CRD leaves the applied set**: deleting a CRD garbage-collects every custom resource of that kind, so a `DataPlane` CRD inside the workload apply turns `kubectl delete -k` into silent tenant-data destruction — the thing SPEC-0039 AC8 forbids and the thing Helm's `crds/` exemption was load-bearing for. It gets its own directory, apply order and gate assertion, the shape CNPG already uses. In-place *update* stays permitted because one stored version (`v1alpha1`, served+storage) makes it safe; the non-destructive proof is deferred to whichever task serves a second version, named now so it is not discovered then. **The bundle payload is the contradiction**: ADR-0096 decision 6 says the Operator is 'unchanged' and reconciles a verified manifest bundle, but `cmd/operator-app/release.go` converges *one image digest* (`CanonicalIdentity()` = `oci_ref@digest`), so decision 6 needs a new payload, verifier and applier in `backend` — which SPEC-0068 puts out of scope. Decision 6's direction is affirmed and T-0085 does **not** build it; until it lands the installer's trust obligation is the existing per-image digest chain, which is engine-agnostic and survives the conversion untouched. Stated both ways so no gate records a green it did not earn | Proposed |
| [ADR-0104](0104-custody-tls-trust-for-the-control-plane.md) | The control plane cannot verify the custody service's certificate, and nothing in the tree can give it the CA. Found by the first production apply that put a TLS-serving OpenBao on a real cluster. **Two correct halves that do not meet**: `openbao-tls` must be privately signed (its SANs are `openbao-N.openbao-internal`, which no public CA will ever issue for), while the custody adapter's `Config` has no CA field at all and `defaultHTTPClient()` verifies against the system pool — which, under ADR-0035's `FROM scratch` image, is exactly one `ca-certificates.crt`. The installer mounts no CA because no CA variable exists. It survived every gate because the custody TLS branch had **never been taken**: dev serves `tls_disable = true` over loopback HTTP, and the platform gate's `loopback-http` fixture correctly refuses that relaxation reaching production — so both doors are shut and there is no working production configuration. Proposes an explicit `Config.CAFile` + `GITFROK_CUSTODY_CA_FILE` rather than `SSL_CERT_FILE`, which *replaces* the default root set and would silently break every other TLS destination; the CA arrives as its own operator-created `openbao-ca` Secret (`ca.crt` only — never the server key), and a gate asserts that an `https` custody address and a mounted CA always travel together | Proposed |

## Open follow-ups

Tracked *inside* the named ADR; promote to a new ADR when decided. Closed items are not kept here —
the deciding ADR is the record.

| Follow-up | ADR |
|---|---|
| The staleness bound for roles captured at login — the ADR fixes where roles live, not how often they are re-fetched | 0049 |
| The CSRF mechanism for browser-initiated writes. `SameSite=Lax` mitigates but does not close it, and the MR write surface must carry a defence | 0049 |
| Whether distributed artifacts need a `NOTICE` or an SBOM of third-party licences | 0040 |
| **No gate asserts an ADR's status matches what merging it meant.** Proposed on `main` is legitimate — an agent drafts a Proposed ADR and stops, and that ADR merges while it waits. The narrow defect: an ADR whose PR review *was* the approval should merge `Accepted`, and nothing distinguishes that from propose-and-stop. It is a question about intent, so it belongs in `.github/pull_request_template.md`, not in `check-docs.sh`. Has caught out ADR-0038, ADR-0039 and ADR-0040 | 0001 |
| The *toolchain* half of the version-floor gate — assert installed `go`/`node`/`tsc` meet their floors in CI. The *image* half closed as ADR-0034 | 0089 |
| Per-OS Minikube driver docs, and CI that exercises the dev-cluster flow itself | 0024 |
| Event catalog and naming — names exist as the protobuf full names of `contracts/events`, but nothing documents them | 0022 |
| Super-repo CI: fail on submodule pointers referencing unmerged commits | 0027 |
| Per-repo scaffolding + generated-type publishing (contracts → TS). Blocks **per-consumer** codegen gating: a consumer's `buf.gen.yaml` reads `../governance/contracts`, which exists only in the composition, so freshness is gated at the super-repo pin instead | 0027, 0028, 0032 |
| Meter audit-store and evidence-pack growth against the PRD §6 fair-use dimensions — retention is now unbounded by decision, and nothing measures it; ADR-0062 joined the agent and residency store tables to the same follow-up | 0055 |
| Whether an expired attested store leaves a marker distinguishable from "never imported" | 0055 |
| PRD revision adding AI-assisted review as **PR-24**, a roadmap placement, and an inference-volume fair-use dimension — the capability is Accepted with no requirement behind it | 0057 |
| Platform-wide tenant secret custody, expected to **supersede** ADR-0057 decision 5 once a second consumer (webhooks, registry, mirrors) exists — ADR-0066 records OpenBao as its presumptive home but authorizes no second consumer | 0057, 0066 |
| OpenBao pin-and-upgrade cadence: upstream maintains only its latest major, and 2.7 moves several built-in seals to plugins — a packaging change to track before it lands. An operational obligation with no end date, deliberately not a Phase 3.1 acceptance criterion | 0066 |
| Hardware-backed signing for the agent CA — OpenBao's External Keys RFC (transit/PKI keys backed by KMS/HSM) is Accepted upstream but not landed. A future compliance bar demanding it is a new decision on the same custody seam, not a change to ADR-0064's posture | 0066, 0064 |
| What the tenant-scoped `platform_operator` role may and may not accumulate — ADR-0046 confined it to one action, ADR-0067 added a second; the general rule is worth writing before a third arrives | 0046, 0067 |
| Door authentication and a server-derived tenant-pinning interceptor for the Phase-2 dataplane door — SPEC-0002's recorded limit (d). SPEC-0043 AC6 gives the new `residency/v1` surface a verified caller; whether that seam generalizes to the older doors is undecided | 0045, 0006 |
| How a non-deterministic producer's output may be cited as control evidence — until decided, AI review stays out of an evidence pack's scan-gate sections | 0057 |
| AISVS L3 obligations that do not exist yet: nonce-bound approvals (C9.2.8), per-execution budgets (C9.1.2), tool-definition snapshot with re-approval (C10.4.8), indirect-prompt-injection screening (C10.4.2), out-of-band kill switch (C9.6.3) — agent cryptographic identity (C9.4.1) closed by ADR-0060 + ADR-0064 | 0056, 0057 |
| Amend SPEC-0010 and SPEC-0020 (both Approved) for the pipeline format, publish the supported-construct subset, and file the implementing task | 0058 |
| Retention and fair-use metering of stored scan reports — SPEC-0037 bounds size and age, but nothing meters growth, and ADR-0055's retention rules do not cover a report (it is not an audit record) | 0059 |
| Whether to adopt Woodpecker's **engine** later — headless executor or Go library inside `modules/ci`. Not foreclosed by adopting the format | 0058 |
| HTTP/2 proxy fallback for a customer whose egress permits only a proxy — the remaining half of ADR-0017's follow-up; cert issuance closed by 0060 | 0017 |
| Unit-economics model per pricing tier — dimensions and derivation now fixed by 0061, prices still open | 0008 |
| Which PRD §6 dimensions are centrally derivable (storage and index size are sizes, not events) — SPEC-0041 states coverage; the rest is deferred and must not read as metered | 0061 |
| Teach `check-ceremony-tier.sh` to read the tier from a pushed commit, so SPEC-0012's declaration is checked rather than only written | 0053 |
| Whether a red `main` should notify anything beyond whoever pushed it | 0053 |
| First-party images are pinned by tag, not digest, in `deploy/dev` | 0035 |
| **The data-plane door's published contract** — routes, TLS requirements, and what a customer must satisfy for the install to be supported. Without it, ADR-0094 decision 1 is an instruction to improvise. **Blocks a supported BYO install** | 0094, 0039, 0013 |
| Extend `CheckNoDataPlaneDial` to the `bff` repo. The invariant holds there today only because `bff/` sits outside the gate's walk of `cmd/controlplane-app` and its imports — true by repository layout rather than by assertion | 0094, 0011 |
| Where the data plane's door URL lives on the enrolment record, and what the control plane renders when it is absent — a tenant with no exposed door has a billing surface and no repository surface, and that state must render as itself rather than as an error | 0094, 0038 |
| Whether the vendor's own reference data plane exposes its door publicly or only to the operator network, given ADR-0092 gave prod-dp a private endpoint and no authorized networks | 0094, 0092 |
| GitOps for applying the control-plane **overlay** (ADR-0096 replaced the chart). Never decided rather than decided-and-lapsed: Argo CD appears only in ADR-0019, which is Superseded. ADR-0093 decision 5 picks the release pipeline in the meantime, and ADR-0096 decision 8 keeps it | 0093, 0096 |
| Whether the runbook's three manual cold-start seams — schema migrations, the Zitadel OIDC client, and OpenBao's quorum unseal — get a production procedure distinct from the Minikube one they are written against | 0093, 0066, 0024 |
| Whether `.tool-versions` should carry the OpenTofu and Terragrunt floors, so `check-version-floors.sh` gates them rather than leaving the constraint inside `root.hcl` where no gate reads it | 0092 |
| Backup and restore for the in-cluster stateful set ADR-0092 decision 5 keeps ours — the decision accepts the obligation and nothing yet discharges it | 0092 |
| **Workload Identity Federation for the image-publish workflow.** ADR-0098 decision 5 keeps ADR-0047's rule that only a protected workflow from reviewed `main` or a `v*` tag may publish, and changes its credential to keyless GCP federation with **no service-account key ever created**. Until the pool, provider and bindings exist, publishing is a manual act by whoever holds the credentials — the state ADR-0047's authority rule was written to prevent | 0098, 0047, 0092 |
| **Egress budget for public image pulls.** A public GHCR package was GitHub's bandwidth; a public Artifact Registry repository is ours, billed per GB to `gitfrok-prod-cp`, and every BYO customer's pull path now runs through it. Unmetered and unbudgeted as of ADR-0098 | 0098, 0008 |
| Whether `check-dev-images.sh` should assert that the **production** image references resolve, now that they will. Its first-party exemption was correct while `dev-up.sh` built into the Minikube node and nothing was published, and it is what hid `versions.env`'s `docker.io` reference pointing at four images that returned 404 | 0098, 0035 |
| Whether a second Artifact Registry repository or a mirror is warranted. One regional repository is a single point of failure for every install, and Docker repositories have no multi-region mode in this shape | 0098, 0013 |
| **`check-custody-service.sh` proves properties about a Minikube file.** It hardcodes `deploy/dev/openbao.yaml`, so once ADR-0099's production OpenBao exists the gate asserts three replicas, Shamir-only unseal and credential-freedom about a manifest that is not the one running. SPEC-0069 AC7 requires teaching it the production path, and the implementation trap is a path list that silently matches nothing and passes | 0099, 0066 |
| **The upgrade ORDER across Postgres, Zitadel and the control-plane app.** Each cares about the others and nothing states the sequence, so whoever first upgrades production discovers it | 0099, 0093 |
| **Backup retention and the PITR window** for ADR-0099 decision 5's bucket. The decision makes backups exist and leave the cluster; it does not say for how long or how far back | 0099, 0092 |
| **SeaweedFS replication on the data plane.** ADR-0099 decision 4 states a single replica is adequate for ADR-0050's large-object scope and is explicitly **not** a durability answer | 0099, 0050 |
| Whether Valkey's single replica is revisited. ADR-0099 decision 4 chose it because session loss is recoverable where data loss is not; it closes when someone names a cost for logging every user out | 0099, 0052 |
| **The four control-plane door registrations** ADR-0100 decision 1 requires — `PolicyDecisionPoint`, `EvidenceService`, `AuditorGrantService`, `OIDCLogin` — on `controlplane-app`'s existing listener set. Each module is already wired and in-process there; only the registration is missing. **SPEC-0070 AC5/AC6 cannot be met until this lands**, because a control-plane BFF has nothing to call | 0100, 0094, 0041 |
| **A control-plane notification has nowhere to live.** ADR-0100 decision 3 kept notifications data-plane-side because their events originate in code review and CI; a notification about billing, a grant or a residency change therefore has no home. None exists, so nothing regresses — it is a gap in the surface ADR-0086 built | 0100, 0086 |
| **Two audit trails, with nothing joining them.** ADR-0100 decision 4 makes bi-planar `audit` the design, so an auditor asking what happened must read both, and no read spans them | 0100, 0055 |
| **The `GITFROK_PDP_ADDR` rename** to `GITFROK_DATAPLANE_ADDR` (ADR-0100 decision 5), additive for one release — and the release at which the old name stops being accepted. A name asserting one service while carrying fourteen is how SPEC-0070 mis-sized its own problem | 0100, 0041 |
| **No production migration mechanism exists.** ADR-0093 decision 4 named schema migrations as the first of the runbook's three manual seams, written against Minikube; `dev-provision.sh` applies twelve migrations to one database and is dev-only. ADR-0101 decision 4 fixes what such a mechanism must PRODUCE — the exact per-plane subset — and builds none of it. **Blocks a first deployment** | 0101, 0093, 0024 |
| **A gate asserting schema ownership** — a plane's migration subset must contain no schema ADR-0101 decision 1 gives the other plane. Without it, decision 1's useful corollary (a data-plane database holding an `agent` schema is misconfigured) is unenforced | 0101 |
| **An evidence pack is structurally incomplete about the other plane.** ADR-0101 decision 2 splits `audit`, `identity` and `policy` into disjoint per-plane instances, so a pack assembled on one plane cannot speak for the other — and ADR-0029's packs are control evidence. Whether an auditor needs a joined read across the two `audit` instances is now larger than ADR-0100 left it | 0101, 0029, 0055 |
| **Whether `OIDCLogin` belongs control-plane-side at all.** ADR-0100 decision 1 assigned it and ADR-0101's schema measurement did not contradict that — but the control plane has never had an OIDC verifier configuration, and `identity`'s credential store is bi-planar, so login could arguably work either side. The weakest of the four assignments | 0101, 0100, 0045 |
| A staging environment. The `live/` hierarchy is shaped for one and only `prod-cp` and `prod-dp` exist | 0092 |
| **Reserved static addresses have no unit.** ADR-0095 decision 6 requires a `google_compute_global_address` for the Gateway and a `google_compute_address` for the agent door, because GKE hands both ephemeral addresses and a hand-written record then advertises an address that moved. The ADR requires them and does not place them — extend `network` or add an `addresses` unit. **Blocks applying ADR-0095's surface** | 0095, 0092 |
| **A live DNS gate for the agent door's grey-cloud record**, which is the only thing making ADR-0095 decision 5 a property rather than a sentence: `dig +short agents-gitfrok.7.solutions` must return the reserved address, never a Cloudflare anycast one. A manifest check cannot see a dashboard click, and Cloudflare's dashboard defaults new A records to proxied — which would break every enrolment while looking like a security improvement | 0095, 0017 |
| **`gke-cluster` sets no `gateway_api_config`** (verified absent from `deploy/gcp/modules/gke-cluster/main.tf`). ADR-0095 decision 2's Gateway renders without it but cannot be applied to a cluster lacking the CRDs. **Blocks applying ADR-0095's surface** | 0095, 0092 |
| **Retire the Cloud DNS unit.** ADR-0095 decision 10 leaves `deploy/gcp/modules/dns-zone/` and `live/prod-cp/dns-zone/` provisioning a zone nothing resolves, and `dns_name` in `env.hcl` moot. A dead unit is a defect, not a curiosity; removal is a super-repo commit | 0095, 0092 |
| **Rotate the Cloudflare Global API Key.** The credential in the operator's tree is 37 hex characters — account-root, un-scopeable to a zone or to DNS, and it sat untracked-but-unignored. ADR-0095 decision 9 bars it from cluster, OpenTofu and Secret alike and uses it for one manual record creation; rotating it is cheap and unrelated to whether the deployment proceeds | 0095 |
| A cert-manager **1.15** floor for ADR-0095 decision 7's Gateway API HTTP-01 solver, and whether `check-version-floors.sh` should carry it | 0095 |
| Whether the browser and issuer hosts later re-point at Cloudflare-**proxied** records, buying back the edge for the half that can take it. ADR-0095 decision 4 leaves this open deliberately; the agent door can never join it (decision 5) | 0095 |
| **What an overlay may vary**, stated before a second overlay exists. Overlay proliferation is Kustomize's failure mode the way values sprawl is Helm's, and ADR-0092 already shapes `live/` for staging. SPEC-0067 AC11 states the rule for the one overlay there is | 0096, 0092 |
| **Recording the applied revision and image digests per apply.** ADR-0096 decision 8 replaced `helm rollback` with re-applying a pinned revision, and Kustomize keeps no release history in the cluster — without this recording, rollback is archaeology | 0096 |
| Whether `deploy/dev` converges on ADR-0096 decision 2's base. It is the only thing that would make ADR-0092 decision 5's "one production shape testable against the dev shape" literally true, and ADR-0096 decision 10 explicitly does not decide it | 0096, 0024, 0092 |
| **The Zero Trust connector VM and its tunnel token** — ADR-0097 decisions 2–4 require a GCE connector per environment, a Secret Manager container whose value OpenTofu never sees, an IAM binding, and a runbook seam for populating the token. Until it lands, both clusters have private APIs and **no operator path to them at all**; the GCP API still answers, the Kubernetes API does not | 0097, 0092 |
| **Where the Cloudflare-side Zero Trust configuration lives** — the tunnel, the Access application, its policies and the private-network route are created in a vendor console or by API, not in this tree. Whether that is captured as code, and where, is undecided. It is the same question ADR-0095 decision 4 left hanging when it put three DNS records in the same console | 0097, 0095 |
| **CI's path to the Kubernetes API.** ADR-0097 authorizes a human through Access and WARP; a pipeline is neither, and nothing decides whether it gets a service token, its own connector, or no access | 0097 |
| Whether ADR-0097 decision 5's break-glass gets a rehearsal. An untested fallback is a hope, and this one is the only path back to the API if Access is unavailable | 0097 |

| **ADR-0096 decision 6 is still open after T-0085 finishes.** ADR-0103 decisions 6-8 affirm the direction — the release artifact becomes the signed rendered manifest set — and decline to build it, because the Operator converges one image digest and a bundle needs a new `.release` payload, a verifier that hashes it, and an applier that applies a set. A `backend` task under invariant 23. Until it lands, what is signed is still an image identity, not what is applied | 0103, 0096, 0044, 0013 |
| **The second served CRD version is where in-place update becomes destructive.** ADR-0103 decision 4 permits in-place update only because `v1alpha1` is the single stored version; serving a `v1beta1` requires a conversion strategy and a storage migration of every stored `DataPlane`, and getting it wrong orphans tenant objects rather than failing loudly | 0103, 0039 |
| **Nothing asserts that out-of-band CRD application is one shape rather than two exceptions.** ADR-0103 decision 1 gives the `DataPlane` CRD the lifecycle `deploy/k8s/platform/operators/cloudnative-pg/` already uses, on a customer's cluster instead of ours. The consistency is intended and unenforced | 0103, 0099 |
| **The custody CA has no rotation story.** ADR-0104 decision 4 puts `ca.crt` in an operator-created Secret and stops there. Rotating it is two-sided — new bundle, control plane restarted — and wants the overlap discipline ADR-0044 already applies to release keys. Named now so the second rotation is not the first time anyone thinks about it | 0104, 0066, 0044 |
| **Who generates the custody CA in steady state.** Today: a human, out of band, once per cluster. cert-manager with a cluster-internal issuer would automate issuance and renewal and would still need ADR-0104 decisions 1-2 to be usable, because it changes who signs and not who can verify | 0104, 0066 |
| **`check-custody-service.sh` asserts references, not transport.** It proves no data-plane surface *mentions* custody, which is why ADR-0104's gap should not apply data-side. That is an argument rather than an assertion, and the two differ exactly when someone adds a data-plane custody client | 0104, 0066 |